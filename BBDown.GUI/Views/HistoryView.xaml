<UserControl x:Class="BBDown.GUI.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="White"
            CornerRadius="8"
            Padding="18"
            BorderBrush="#FFE0E0E0"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="历史记录"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,6" />
                    <TextBlock Text="记录完成和失败任务，可打开目录或载入到下载页重新执行。"
                               Foreground="#FF666666"
                               TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Content="打开目录"
                            Command="{Binding History.OpenDirectoryCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" />
                    <Button Content="重新下载"
                            Command="{Binding History.RetryCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" />
                    <Button Content="删除"
                            Command="{Binding History.DeleteCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" />
                    <Button Content="清空"
                            Command="{Binding History.ClearCommand}"
                            Style="{StaticResource SecondaryButton}" />
                </StackPanel>
            </Grid>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding History.Records}"
                      SelectedItem="{Binding History.SelectedRecord, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="完成时间" Binding="{Binding FinishedAt, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="150" />
                    <DataGridCheckBoxColumn Header="成功" Binding="{Binding IsSuccessful}" Width="60" />
                    <DataGridTextColumn Header="标题" Binding="{Binding Title}" Width="*" />
                    <DataGridTextColumn Header="来源 URL" Binding="{Binding Url}" Width="220" />
                    <DataGridTextColumn Header="输出目录" Binding="{Binding OutputPath}" Width="160" />
                    <DataGridTextColumn Header="错误" Binding="{Binding ErrorMessage}" Width="220" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
